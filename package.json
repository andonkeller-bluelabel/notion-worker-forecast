{
  "name": "notion-worker-forecast",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Notion Worker that spreads Deal Revenue Schedule segments into monthly revenue (committed + weighted) and writes a tidy fact table to the Forecast Dashboard Google Sheet for native pivots.",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test:forecast": "tsx src/lib/forecast.test.ts",
    "dev": "ntn workers dev",
    "deploy": "ntn workers deploy"
  },
  "dependencies": {
    "@notionhq/workers": "^0.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.23.0",
    "typescript": "^5.6.0"
  }
}
